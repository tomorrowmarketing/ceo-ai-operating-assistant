# 10. Notion Integration

Notion integration is disabled.

The app now uses mock data unless `ENABLE_NOTION_INTEGRATION=true` is explicitly
set. Do not configure `NOTION_TOKEN` or `NOTION_DB_*` values unless the Notion
feature is intentionally restored.

To fully retire existing credentials, revoke the Notion integration token in the
Notion workspace integration settings.
